# Archive Directory

This directory contains archived snapshots and non-canonical artifacts.

## nyra.zip

**Status:** Not currently present

**Purpose:** If `nyra.zip` exists, it should be placed here as an archive snapshot.

**Note:** This is NOT the canonical source of truth. Repository code in `src/` and specs in `spec/` and `subsystems/` are authoritative.

## Cleanup Date

2025-12-27 - Repository hygiene cleanup to prevent accidental commits of large binary files. Root-level `nyra.zip` files are now ignored via `.gitignore` (except when in `archive/`).
